## MUSEUM
slot_xhk_extraframe_museum = {
	start = zone_xhk_extraframe_museum
	include = {                             # A list of zone types that are available to be built in the slot. The key 'all' can be used to include all existing zone types.
		zone_xhk_extraframe_museum
	} # targeting directly

	unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
		always = yes
	}
}

#KNIGHTS
slot_xhk_extraframe_knight = {
	start = zone_xhk_extraframe_knight
	include = {                             # A list of zone types that are available to be built in the slot. The key 'all' can be used to include all existing zone types.
		zone_xhk_extraframe_knight
	} # targeting directly

	unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
		always = yes
	}
}

#KNIGHTS
slot_xhk_extraframe_storage = {
	start = zone_xhk_extraframe_storage
	include = {                             # A list of zone types that are available to be built in the slot. The key 'all' can be used to include all existing zone types.
		zone_xhk_extraframe_storage
	} # targeting directly

	unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
		always = yes
	}
}

#BIRCH
slot_xhk_extraframe_birchextra = {
    start = zone_xhk_extraframe_birchextra
    include = {
        zone_xhk_extraframe_birchextra
    }
    unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
        always = yes
    }
}
slot_xhk_extraframe_orykta = {
    start = zone_xhk_extraframe_orykta
    include = {
        zone_xhk_extraframe_orykta
    }
    unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
        always = yes
    }
}
slot_xhk_extraframe_ktisma = {
    start = zone_xhk_extraframe_ktisma
    include = {
        zone_xhk_extraframe_ktisma
    }
    unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
        always = yes
    }
}
slot_xhk_extraframe_physma = {
    start = zone_xhk_extraframe_physma
    include = {
        zone_xhk_extraframe_physma
    }
    unlock = {                              # Trigger that defines whether the slot is unlocked on the planet. Scope = Planet.
        always = yes
    }
}